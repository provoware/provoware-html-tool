{
  "name": "provoware-html-tool",
  "version": "0.1.0",
  "private": true,
  "description": "Modularer JSON-Store mit Self-Repair und Hilfe-Panel",
  "scripts": {
    "format": "npx prettier --write \"**/*.{js,json,md,html}\"",
    "test": "node --test",
    "system:test": "node tools/help_cli.js test"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
