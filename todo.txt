# TODO – provoware-html-tool (optimierter Entwicklungsprozess)

DONE: Doctor-Modus ergänzt (`./start.sh --doctor`) inkl. klarer Verbesserungsbefehle und zusätzlicher Test-Ausgabe-Validierung. (2026-02-22)
NEXT: Theme-Konfiguration nach `config/themes.json` auslagern und im Release-/Doctor-Check mit automatischer Kontrastmeldung auswerten. (2026-02-22)

## Iteration 2026-02-22 (Doctor-Modus + robuste Test-Rückmeldung)
### A) Fundstelle (beobachten)
- Problem: Es fehlte ein klarer Modus, der konkrete Verbesserungen als vollständige Befehle ausgibt; außerdem bestätigte der Selbsttest den Ausgabe-Erfolg nicht explizit.
- Risiko: Nutzer wissen nicht eindeutig, welcher nächste Schritt sinnvoll ist; Testläufe könnten stillschweigend unklare Ergebnisse liefern.
- Erwartung: Ein neuer Doctor-Modus liefert verständliche Handlungsschritte, und der Selbsttest bestätigt aktiv eine vorhandene Ausgabe.

### B) Change-Scope (vor Patch)
- Ziel: Diagnosemodus für Verbesserungen bereitstellen, Test-Ausgabe validieren und Hilfe-/Textsystem um barrierefreie Hinweise erweitern.
- Dateien: `start.sh`, `README.md`, `CHANGELOG.md`, `todo.txt`, `data/version_registry.json`
- Patch-Block je Datei: jeweils ein zusammenhängender Änderungsblock.
- Abnahmekriterium „fertig“: `./start.sh --doctor` liefert klare Befehle; `./start.sh --test` bestätigt Testausgabe; Hilfe zeigt den neuen Modus.

### C) Patch (kurz)
- Punkt 1 – Änderung: Neuer Modus `--doctor` mit strukturiertem Verbesserungsbericht und vollständigen Befehlen.
- Punkt 2 – Änderung: `run_tests` prüft zusätzlich, ob Protokollausgabe vorhanden ist, und liefert bei Problemen klare Next Steps.
- Punkt 3 – Änderung: Hilfe- und Textbausteine für den Doctor-Modus ergänzt (barrierefreundlich mit einfacher Sprache).

### D) Gates
- G1: `python -m compileall -q .` (grün)
- G2: `bash tools/run_quality_checks.sh` (rot: Datei fehlt im Repo)
- G3: `python tools/smoke_test.py` (rot: Datei fehlt im Repo)
- G4: `bash start.sh` (grün)
- G5: Mini-UX-Check (grün im betroffenen Bereich: klare deutsche Dialoge, Next Steps vorhanden, kein Crash, Kontrast-/A11y-Hinweise vorhanden)

### E) Ergebnis
- Status: NEXT ITERATION (zwei Pflicht-Gates fehlen als Dateien im Repo)
- Doku: README + CHANGELOG + todo aktualisiert
- Laienvorschläge:
  1. Nutze `./start.sh --doctor`, um sofort konkrete Befehle für den nächsten Schritt zu bekommen.
  2. Starte bei Unsicherheit immer `./start.sh --check --debug` und öffne danach `cat logs/start.log`.
- Nächster Schritt: Lege `tools/run_quality_checks.sh` und `tools/smoke_test.py` als kleine Standard-Skripte an, damit alle Pflicht-Gates automatisch vollständig grün laufen.
