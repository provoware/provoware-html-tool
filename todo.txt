# todo.txt

## Ziel der aktuellen Iteration
Die nächsten offenen Punkte aus P2/P3 in kleine, direkt umsetzbare Doku-Schritte überführen.

## Aktueller Fortschritt
- Erledigte Punkte gesamt: 4
- Offene Punkte gesamt: 4
- Entwicklungsfortschritt: 50%

## Priorisierte offene Punkte (geordnet)

### P2 – Umsetzungsphasen klein und sequenziell halten
- **Problem:** Arbeitspakete sind definiert, aber noch nicht praktisch gestartet.
- **Patchgrund:** Ohne Startpaket bleibt die Planung stehen.
- **Nächster Schritt:** Arbeitspaket 1 (`index.html`, `assets/css/base.css`, `assets/js/core.js`) als Minimalstart umsetzen.
- **Prüfschritt:** Dateien vorhanden, Offline-Start sichtbar.
- **Fertig-Kriterium:** Dashboard-Skelett lädt lokal ohne Zusatzdienste.

### P3 – Start-Minimum klar gegenüber späteren Dateien abgrenzen
- **Problem:** Muss-Dateien und spätere Dateien sind noch nicht vollständig getrennt dokumentiert.
- **Patchgrund:** Klare Abgrenzung senkt Pflege- und Umsetzungsrisiko.
- **Nächster Schritt:** Für jede Startdatei genau einen Zweck-Satz ergänzen.
- **Prüfschritt:** Jede Startdatei hat eine klare Kurzbeschreibung.
- **Fertig-Kriterium:** Keine unklaren oder redundanten Startdateien.

### P4 – Modulstandard `datenbank_baukasten` als Muster fixieren
- **Problem:** Muster ist geplant, aber nicht konkret als Datei-Set angelegt.
- **Patchgrund:** Einheitliches Muster verhindert Sonderwege.
- **Nächster Schritt:** Minimales Modul-Dateiset dokumentiert/angelegt vorbereiten.
- **Prüfschritt:** `manifest`, `config`, `texts`, `schema`, `logic` eindeutig vorhanden/beschrieben.
- **Fertig-Kriterium:** Muster kann 1:1 für weitere Module kopiert werden.

### P5 – Doku-Landkarte klein halten
- **Problem:** Doku wächst schnell ohne klare Priorität.
- **Patchgrund:** Kleine Doku hält Iterationen stabil.
- **Nächster Schritt:** Nur verhaltensrelevante Doku aktiv pflegen (`README.md`, `todo.txt`, `AGENTS.md`, `TOOL_TUTORIAL.md`, `INDEX.md`).
- **Prüfschritt:** Keine leeren Platzhalter-Dokus im Repo.
- **Fertig-Kriterium:** Dokuumfang bleibt schlank und direkt nutzbar.

## Diese Iteration erledigt
1. AGENTS-Struktur gestrafft und Index-Pflicht ergänzt.
2. `todo.txt` auf nächste offene Punkte neu fokussiert.
3. `INDEX.md` neu angelegt (Baum + vollständige Dateiliste).
4. `TOOL_TUTORIAL.md` neu angelegt und um Index-Workflow erweitert.

## Geparkte Folgepunkte (bewusst nicht Teil dieser Iteration)
- Feingranulare Logging-Strategie für mehrere Logdateien.
- Self-Repair-Ausbau über Minimalbasis hinaus.
- Erweiterte Export- und Archivregeln.
