# TODO – provoware-html-tool (optimierter Entwicklungsprozess)

DONE: Theme-Konfiguration in `config/themes.json` eingeführt, Start-/Template-Validierung verknüpft und Dashboard-Template mit Skip-Link + verbessertem Fehlerdialog barriereärmer gemacht. (2026-02-22)
NEXT: Theme-Farbwerte in eine zentrale Datei auslagern und einen automatischen WCAG-Kontrasttest als eigenen Tool-Check ergänzen. (2026-02-22)

## Iteration 2026-02-22 (Theme-Config + A11y + Smoke)
### A) Fundstelle (beobachten)
- Problem: Theme-Werte waren nicht zentral konfiguriert und die A11y-Absicherung war im Smoke-Test noch nicht vollständig.
- Risiko: Uneinheitliche Themes, unklare Fehlermeldungen und spätere Regressionen bei Barrierefreiheit.
- Erwartung: Konfigurierbare Themes, robuste Prüfung und klar verständliche Nutzerhinweise mit Next Steps.

### B) Change-Scope (vor Patch)
- Ziel: Theme-Konfiguration zentralisieren, Template-A11y verbessern und automatische Prüfung dafür ergänzen.
- Dateien: `start.sh`, `templates/dashboard_musterseite.html`, `tools/smoke_test.py`, `config/themes.json`, `README.md`, `CHANGELOG.md`, `todo.txt`, `data/version_registry.json`
- Patch-Block je Datei: jeweils ein zusammenhängender Block.
- Abnahmekriterium: `./start.sh --dashboard-template` prüft Theme-Optionen aus Konfiguration, Smoke-Test validiert A11y-Marker + Theme-Datei erfolgreich.

### C) Patch (kurz)
- Punkt 1 – Änderung: Theme-Konfiguration in `config/themes.json` eingeführt und in `start.sh` für GUI-/Template-Validierung angebunden.
- Punkt 2 – Änderung: Dashboard-Template um Skip-Link, `aria-modal` und klarere Fehler-Next-Steps erweitert.
- Punkt 3 – Änderung: Smoke-Test um Theme-Konfigurationsprüfung, Namensvalidierung und A11y-Marker ergänzt.

### D) Gates
- G1: `python -m compileall -q .`
- G2: `bash tools/run_quality_checks.sh`
- G3: `python tools/smoke_test.py`
- G4: `bash start.sh`
- G5: Mini-UX-Check (deutsche Dialoge, Fehlerdialog mit Next Steps, kein Crash, Kontrast/Theme geprüft)

### E) Ergebnis
- Status: DONE
- Doku: README + CHANGELOG + todo aktualisiert
- Laienvorschläge:
  1. Öffnen Sie die Musterseite im Browser und drücken Sie zuerst Tab, damit Sie den neuen Sprung-Link (Skip-Link) direkt sehen.
  2. Wählen Sie nacheinander alle Themes aus und klicken Sie dann auf „Beispiel-Fehler anzeigen“, um die nächsten Schritte im Dialog zu prüfen.
- Nächster Schritt: Legen Sie eine Datei für Theme-Farbwerte an, lesen Sie sie automatisch im Template-Build ein und ergänzen Sie danach einen messbaren Kontrasttest, damit jede Farbkombination dauerhaft barrierefrei bleibt.
