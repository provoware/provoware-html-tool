# TODO – provoware-html-tool (optimierter Entwicklungsprozess)

DONE: Mini-UX-Autocheck (`--ux-check-auto`) ergänzt, Theme-Hilfetext für Screenreader erweitert und Smoke-Test um UX-Prüfung ergänzt. (2026-02-22)
NEXT: WCAG-Kontrasttest (automatische Kontrast-Prüfung) als eigenen Gate-Schritt ergänzen und CI-Ausführung anbinden. (2026-02-22)

## Iteration 2026-02-22 (Full-Gates + Statusbericht + Hilfe)
### A) Fundstelle (beobachten)
- Problem: Pflicht-Gates liefen nicht als strikt geketteter Startmodus, und ein leicht vorlesbarer Gesamtstatus fehlte.
- Risiko: Uneinheitliche Freigabequalität und unklare Hilfe für Nutzer bei Fehlern.
- Erwartung: Ein Befehl für Gates 1-4, klare Statusdatei und sichtbare Next Steps im Template.

### B) Change-Scope (vor Patch)
- Ziel: Vollautomatische Gate-Kette, barrierearmen Statusbericht und Hilfebereich mit festen Lösungen bereitstellen.
- Dateien: `start.sh`, `tools/smoke_test.py`, `templates/dashboard_musterseite.html`, `README.md`, `CHANGELOG.md`, `todo.txt`, `data/version_registry.json`
- Patch-Block je Datei: jeweils ein zusammenhängender Block.
- Abnahmekriterium: `./start.sh --full-gates` läuft erfolgreich, `logs/status_summary.txt` wird erzeugt, Smoke-Test validiert neue Marker und Template-Hilfebereich.

### C) Patch (kurz)
- Punkt 1 – Änderung: Neuer Modus `--full-gates` führt Gates 1-4 strikt nacheinander aus und stoppt mit klaren Next Steps.
- Punkt 2 – Änderung: Startskript erzeugt `logs/status_summary.txt` als einfache, screenreaderfreundliche Ergebniszusammenfassung.
- Punkt 3 – Änderung: Dashboard-Template ergänzt einen eigenen Hilfebereich mit festen, leicht verständlichen Lösungsbefehlen.

### D) Gates
- G1: `python -m compileall -q .`
- G2: `bash tools/run_quality_checks.sh`
- G3: `python tools/smoke_test.py`
- G4: `bash start.sh`
- G5: Mini-UX-Check (deutsche Dialoge, Fehlerdialog mit Next Steps, kein Crash, Kontrast/Theme geprüft)

### E) Ergebnis
- Status: DONE
- Doku: README + CHANGELOG + todo aktualisiert
- Laienvorschläge:
  1. Nutzen Sie zuerst `./start.sh --full-gates`, damit alle Kernprüfungen automatisch und in richtiger Reihenfolge laufen.
  2. Öffnen Sie danach `cat logs/status_summary.txt`, weil dort alle Ergebnisse in einfacher Sprache gesammelt stehen.
- Nächster Schritt: Ergänzen Sie als Nächstes einen automatischen Kontrastrechner, der bei jedem Lauf alle Theme-Farben gegen WCAG-Mindestwerte prüft und bei Fehlern direkt Korrekturvorschläge ausgibt.


## Iteration 2026-02-22 (Mini-UX-Autocheck + Theme-Hilfe + Smoke)
- 3 Punkte abgeschlossen: neuer Auto-UX-Check in start.sh, verbesserte Theme-Hilfe im Template, Smoke-Test-Erweiterung.
- Hilfe/A11y erweitert: Theme-Hilfe mit Screenreader-Bezug und beschreibendem Attribut (`aria-describedby`).
- Nächster technischer Schritt: automatischen Kontrastrechner als zusätzliches Gate einbauen.
